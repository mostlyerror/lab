<p>
  <strong>Title:</strong>
  <%= @book.title %>
</p>

<%= image_tag @book.cover_image.url if @book.cover_image? %>

<p>
  <strong>Description:</strong>
  <%= @book.description %>
</p>

<%= link_to 'Edit', edit_book_path(@book) %> |
<%= link_to 'Back', books_path %>
